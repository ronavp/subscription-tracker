# Postgres connection string — Vercel Postgres, Supabase, or Neon all work fine
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Get from console.anthropic.com — used server-side only, never exposed to the client
ANTHROPIC_API_KEY="sk-ant-..."
